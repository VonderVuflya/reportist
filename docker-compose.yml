services:
  backend:
    build:
      context: ./backend
    ports:
      - "${API_PORT:-3000}:3000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    ports:
      - "${WEB_PORT:-5173}:80"
    depends_on:
      - backend
    restart: unless-stopped
